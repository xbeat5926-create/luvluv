# LUVTrader currently deploys as a plain static frontend and does not require
# runtime environment variables for the homepage.

# If a separate backend/API is added later, expose only public frontend values here.
# Example:
# LUVTRADER_API_BASE_URL=https://your-backend.example.com
